<%= form_for(@account_login) do |f| %>
  <% if @account_login.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account_login.errors.count, "error") %> prohibited this account_login from being saved:</h2>

      <ul>
      <% @account_login.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
