<%= form_for(@report_display) do |f| %>
  <% if @report_display.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@report_display.errors.count, "error") %> prohibited this report_display from being saved:</h2>

      <ul>
      <% @report_display.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
