<%= form_tag(@admin_survey) do |f| %>
  <% if @admin_survey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_survey.errors.count, "error") %> prohibited this admin_survey from being saved:</h2>

      <ul>
      <% @admin_survey.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
