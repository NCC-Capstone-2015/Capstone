<!-- Created by Rapidfire Edited by Drew Olehy -->

<tr id= "survey_<%= survey.id %>">
  <td>
    <% if can_administer? %>
      <%= link_to survey.name, survey_questions_path(survey) %>
    <% else %>
      <%= survey.name %>
    <% end %>
  </td>
  <td>
    <ul class="horizontal-list">
      <li><%= link_to "Answer Questions", new_survey_attempt_path(survey) %></li>
      <% if can_administer? %>
        <li><%= link_to "Results", [:results, survey] %></li>
        <li><%= link_to "Delete", [survey], method: :delete, data: {confirm: 'Are you sure you want to delete this Survey?'} %></li>
      <% end %>
    </ul>
  </td>
</tr>
